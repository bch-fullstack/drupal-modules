<?php
/**
 * Implements hook_theme() to add the template definition.
 **/
function hello_world_controller_theme($existing, $type, $theme, $path) {
  return array(
    'message_list' => array(
      'variables' => array('greetings' => array(), 'title' => '')
    ),
  );
}
